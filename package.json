{
  "name": "parental-control-frontend",
  "version": "1.0.0",
  "description": "Frontend for parental control app",
  "main": "index.js",
  "scripts": {
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  },
  "dependencies": {
    "core-js": "^3.23.1",
    "vue": "^3.2.0",
    "vue-router": "^4.0.0"  // Add vue-router if not present
  },
  "devDependencies": {
    "@babel/core": "^7.15.0",
    "@babel/eslint-parser": "^7.15.0", // Modern replacement for babel-eslint
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-plugin-eslint": "~5.0.0",
    "@vue/cli-service": "~5.0.0",
    "babel-eslint": "^10.1.0",  // Ensure babel-eslint is included
    "eslint": "^6.8.0",
    "eslint-plugin-vue": "^7.0.0",
    "vue-eslint-parser": "^8.0.0", // Ensure vue-eslint-parser is included
    "eslint-loader": "^4.0.0", // If using Webpack with ESLint loader
    "vue-loader": "^16.2.0",   // Required for Vue single file components
    "vue-template-compiler": "^2.6.11"
  },
  "eslintConfig": {
    "root": true,
    "env": {
      "node": true
    },
    "extends": [
      "plugin:vue/vue3-essential",
      "eslint:recommended"
    ],
    "parserOptions": {
      "parser": "babel-eslint"  // Specify babel-eslint as parser
    },
    "rules": {
      "no-console": "off",
      "no-debugger": "off"
    }
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "engines": {
    "node": ">=14.0.0",  // You can specify required Node version
    "npm": ">=6.0.0"
  }
}
